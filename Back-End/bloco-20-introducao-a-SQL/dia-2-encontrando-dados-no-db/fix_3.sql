SELECT CONCAT(title, ' - ', release_year) AS 'Release film' FROM sakila.film;
SELECT CONCAT(title, ' - ', rating) AS 'Parental rating' FROM sakila.film;
SELECT CONCAT(address, ' - ', district) AS 'Address' FROM sakila.address;
